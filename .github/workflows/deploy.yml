name: Deploy test

on:
  pull_request:
    types: [closed]
    branches:
      - main

jobs:
  build-migration:
    if: github.event.pull_request.merged == true
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
